server:
  port: 8081

spring:
  application:
    name: step1-basic-filter

# ==============================================
# [Step 1] Filter Chain 심화 분석 & DelegatingFilterProxy 실습
# ==============================================
# 확인 포인트:
# 1. 커스텀 필터가 어느 시점에 실행되는지 로그로 확인
# 2. SecurityFilterChain 빈 등록 vs 일반 빈 등록 차이
# 3. HttpFirewall → ... → UsernamePasswordAuthenticationFilter 호출 순서
# ==============================================

logging:
  level:
    org.springframework.security: TRACE
    com.security.training.step1: DEBUG
